source :rubygems
gem 'sinatra', '1.1.0'
gem 'thin'

gem 'aws-s3'
#gem 'data_mapper'
#gem 'active_support'

=begin
group :development, :test do
 gem 'dm-sqlite-adapter'
end
group :production do
  gem 'dm-postgres-adapter'
end
=end
